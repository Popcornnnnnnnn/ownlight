# 项目状态

## 阅读说明

这是当前仓库的结构化状态页。后续项目状态以 `.planning/STATE.md` 为准；旧 GSD 规划树已从 active checkout 移除，边界见 `.planning/LEGACY-GSD-ARCHIVE.md`。

**Current focus:** M017 App Store Readiness And Product Maturity is in release closeout. App Store `1.0 (3)` is live outside China mainland, and English-first `1.1 (4)` is `Waiting for Review`. The remaining checkpoint is a clean-history public source release at `Popcornnnnnnnn/ownlight`. The existing `Popcornnnnnnnn/private-moments` development repository stays private. Product behavior remains local-first with opt-in iCloud Sync and user-configured AI.

## Current Position

Current Phase: 017
Current Phase Name: App Store Readiness And Product Maturity
Total Phases: 17
Current Plan: 017-04
Total Plans in Phase: 4
Phase: 017 of 17 (M017 App Store Readiness And Product Maturity) — active on `main`
Status: Active
Last activity: 2026-07-17 — English App Store submission and public source release
Last Activity Description: Build `1.1 (4)` was archived, uploaded, bound to the version, switched to English primary metadata with six ordered English screenshots, and submitted to App Review. App Store Connect now reports `Waiting for Review`; clean-history GitHub publication remains the final release action.
Progress: 100%

## Decisions Made

| Phase | Summary | Rationale |
| --- | --- | --- |
| 017 | Publish public source from a new clean-history `Popcornnnnnnnn/ownlight` repository while keeping the development repository private. | A clean snapshot provides reusable source without exposing stale backup branches, private development history, or previously ignored release material. |
| 017 | iCloud work is split into iCloud Drive repository path, iCloud Backup information, and opt-in CloudKit private sync. The old `codex/apple-native-v1-direction` branch is reference material, not a wholesale merge target. | This keeps real-device data safety intact while still reusing useful CloudKit thinking from the prior Apple-native experiment. |
| 017 | AI & Analysis is now separate from legacy Mac/server sync: provider credentials live only in iPhone Keychain, provider profiles are local metadata, and iOS runs the default generated artifact path. iCloud is the current optional data replication layer; any Mac worker must remain an explicit advanced profile, not the default AI runtime. | This removes the previous coupling between AI features, Mac server availability, and CloudKit/App Store developer-account timing. |
| 017 | Successful AI provider setup must clear stale fallback failure state for that provider. | A provider can be configured and pass Test Connection while still being excluded by an older `needsAttention` fallback record. A successful test or save is the user's explicit recovery signal, so it should re-enable that profile for summary routing without weakening real configuration-error handling. |
| 017 | AI artifact response decoding should be tolerant of common OpenAI-compatible string-list variants, but still reject malformed top-level artifact shapes. | DeepSeek-compatible responses can return list items as small objects rather than pure strings even when JSON mode is enabled. The app should extract safe text from those variants, while preserving `unsupportedResponse` for genuinely broken artifact fields that would make the summary structurally unreliable. |
| 017 | AI artifact-generation failures must not make an already configured provider look unconfigured. | `unsupportedResponse` means the provider returned a response that the app could not turn into a valid artifact for this generation. It should fail or fallback for that artifact, but it is not the same as a missing key, invalid URL, permission, quota, or model setup error. Old artifact-response `needsAttention` records are sanitized on read. |
| 017 | Settings is organized as a local-first phone app: `This iPhone`, `iCloud`, `Storage & Export`, `AI & Analysis`, `Tags`, `Appearance`, `Language`, and `Display`. Manual sync and Mac/server controls are removed from the visible Settings product path. | The product should feel usable as a standalone iPhone app first; sync should become routine background data movement or iCloud, not a daily manual Settings workflow. |
| 017 | Settings visual rhythm follows native grouped Settings density: rows stay single-line and consistent; long explanations move to section footers or deeper pages. | The Settings pages should scan like ordinary iOS settings instead of alternating compact controls with tall metadata rows. |
| 017 | iCloud Settings hides CloudKit diagnostics from ordinary user-facing Settings. | Ordinary users only need to see account state, the `iCloud Sync` toggle, guidance copy, and `Sync Now`. Container IDs, smoke tests, default-zone probes, and similar setup tools remain developer/operator diagnostics, not normal product actions. |
| 017 | CloudKit ordinary mutation enqueue should cover user-visible library changes and active draft metadata, not only new moments. | Favorite, pin, topic/tag management, post-tag assignments, check-ins, ready check-in AI summaries, synced preference changes, and composer/edit draft text metadata are user-visible library state. When `iCloud Sync` is enabled they should enter the CloudKit pending queue automatically; temporary draft media files and AI provider configuration remain local-only. |
| 017 | CloudKit delete/tombstone enqueue must propagate the visible Moment tree, not just the parent Moment row. | A deleted Moment should disappear consistently on the second device, including attached media, comments, generated summary metadata, and post-tag assignments. Editing media attachments also needs explicit CloudKit media create/delete work so the second device converges to the final media list rather than retaining stale attachments or missing newly added ones. |
| 017 | iPhone on-device transcription remembers the last successful Chinese/English content locale and uses it as the next default. | Speech locale and app UI language usually do not change often; remembering the successful locale avoids paying a predictable failed attempt on every future summary while keeping a one-time alternate-locale recovery path. |
| 017 | Local transcription gateway is a separate authenticated helper endpoint, not a Mac server AI job path. | This gives iPhone-direct AI a better advanced transcription option while keeping data replication, legacy server sync, and AI text-provider credentials separate. |
| 017 | Advanced Transcription exposes provider capability, not transport branding: the visible choices are `iPhone On-device` and `OpenAI-compatible Endpoint`; Local Gateway is only a Base URL example and legacy settings auto-migrate. | This avoids making a Mac-local helper feel like a product-level provider while still supporting LAN/Tailscale/Cloudflare/self-hosted OpenAI-compatible transcription endpoints. |
| 017 | Advanced Transcription `Test Connection` saves the working endpoint draft after a successful test. | The user should not have to guess whether to press Save before Test or Test before Save; a successful test is enough evidence to activate that configuration. |
| 017 | iPhone-direct AI prompts explicitly bind generated title/summary/tag language to AI Language or source-dominant language. | OpenAI-compatible providers can default to English when schema and prompt text are English; generated `documentTitle/title` must not drift away from Chinese-dominant input in Auto mode. |
| 017 | AI summary prompts prefer comprehensive, source-grounded coverage; Summary sheet exposes private transcript diagnostics through a low-frequency `Original Text` action instead of inline timeline/detail content. Provider summary decoding must be tolerant of optional document-block fields, and provider token usage should be stored and shown quietly when available. | The summary should be useful for later review and debugging provider/transcription mismatch, while keeping private transcript text and accounting details out of default browsing chrome. |
| 017 | `syncStatus` and `uploadStatus` remain data-layer compatibility fields, but normal Timeline / Detail / Check-ins / Storage & Export UI should not present sync as a primary concept. | The app should feel fully usable as a pure local iPhone app; Mac/server/iCloud are future or optional replication layers rather than daily manual sync surfaces. |
| 017 | The 2026-05-30 UI audit is closed except for the intentionally skipped extreme dynamic-type Settings edge case. Calendar may use a single lightweight month summary row under the grid; Storage copy should keep first-screen risk text short; AI child toggles should visually depend on the global AI capability switch without overwriting saved preferences. | The polish pass should reduce visual confusion and empty space without adding dashboards, tall Settings rows, or management-style sync/status concepts back into the product. |
| 017 | Composer and Edit Moment no longer show H1/H2 keyboard accessory controls. Markdown heading support remains hidden: users can type leading `# ` / `## ` manually, and reading surfaces continue to render those headings. | Visible formatting controls made the capture surface feel like a writing tool and looked awkward above the keyboard. Keeping Markdown as an optional typed syntax preserves compatibility without adding UI weight. |
| 017 | Composer v2 keeps the main layout but changes the media row to `Photos`, `Camera`, `Audio`, and `More`; `More` owns Video, Scan, and Files. Scan produces image drafts, Files imports supported image/video/audio files plus single PDF document attachments, and Date/Tag are a compact context strip. | This keeps frequent capture actions one tap away while making lower-frequency document/file entry available without crowding the first screen; PDF remains a file preview path, not default image conversion. |
| 017 | Timeline Memory Links are intentionally tiny same-day jump links, not large memory cards or daily notifications. They surface at most one meaningful older moment in the main timeline, use local-only display/open history to avoid fatigue, and can be disabled from Settings > Display. | The feature should add serendipitous recall without turning the timeline into a reminder feed. Quality scoring and frequency rules keep low-signal check-ins from dominating, while the Settings toggle respects users who do not want automatic resurfacing. |
| 017 | Server-published `review-...` Timeline moments are treated as historical weekly review artifacts on iPhone. Calendar > Reviews bridges them into local `localWeeklyReviews` on reload/refresh instead of depending on the old Mac/server review list path. | This fixes the visible split where a scheduled server review appears in Timeline after sync but not in Calendar Reviews, while keeping future regenerate/edit behavior on the iPhone provider path. |
| 017 | The legacy Mac/server automatic Weekly Review scheduler is disabled by default. iOS no longer refreshes or pushes Weekly Review toggle state through `/api/v1/reviews/settings`; those switches are local-only product preferences for the iPhone-direct path. | Mac should not keep producing AI artifacts behind the phone-first architecture. Server review routes and historical rows can remain for compatibility, but they must not schedule new artifacts unless an operator explicitly opts into the legacy scheduler. |
| 017 | Calendar `Reviews` is hidden for AI-off users with no historical weekly reviews, while historical reviews remain visible. `Publish Weekly Review` depends on `Auto-generate Weekly Review` and cannot be enabled as a standalone scheduled action. | Weekly Review is an AI-generated artifact, not a default Calendar concept for every new user. Keeping old reviews visible protects data continuity, while constraining publish prevents a confusing toggle that sounds active even when no automatic review will be generated. |
| 017 | Primary Tag is removed from the normal product surface. Topic tags are grouped under seven fixed Areas only; AI outputs `{area, topics}` and must reuse existing topic names/aliases/compact matches before creating new topic tags. Missing, legacy, or unknown area values are classified into a fixed Area before storage. | The user no longer wants to manually choose primary tags, and flat topic lists were becoming difficult to browse/filter. Fixed Areas preserve lightweight organization without creating another uncontrolled tag vocabulary or a visible catch-all bucket. |
| 017 | First release-stability slice prioritizes standalone iPhone trust while bringing CloudKit private sync into first-phase planning and implementation readiness. `This iPhone` remains the product default, iPhone local export is a verification/future migration package rather than an implemented restore path, and CloudKit must stay opt-in until real-device UAT proves it is safe. | The code has already moved phone-first, but the first App Store track should now include iCloud decisions because the user intends to implement CloudKit after Apple Developer account setup. |
| 017 | Empty-library iPhone local archive import is the next recovery closure step. `Storage & Export` can import the current local archive zip only when the target iPhone library has no existing local records or outbox operations. It restores archive IDs/timestamps/generated metadata and local media files, but does not merge, overwrite, recreate sync runtime state, or import credentials/private transcript text. | The previous export-only state could verify data leaving the phone but not prove a phone-side recovery loop. Empty-library import closes the first recovery drill while keeping non-empty restore and cross-device convergence out of this slice. |
| 017 | App Store readiness work is tracked in `docs/APP-STORE-READINESS.md`. | Up-front Apple requirement research should precede product decisions and implementation. The readiness work needs one checklist that can be updated item-by-item as account policy, privacy URLs, App Privacy labels, onboarding, metadata, TestFlight, accessibility, and release gates are completed. |
| 017 | v1 App Store build will have no account system, registration, or login. | Apple does not require apps to have accounts, and this decision fits the local-first private timeline positioning while avoiding account deletion, server-side account data, and login review complexity. Future cloud sync or subscription work must revisit account/deletion requirements before submission. |
| 017 | v1 App Store AI is user-hosted/user-selected only, not a developer-hosted default AI service. | This keeps the first release aligned with local-first privacy, avoids default AI inference cost and abuse controls, and makes App Review / privacy copy clearer: core capture works without AI, and enabling AI means the user chooses the provider or endpoint and stores credentials locally. |
| 017 | v1 App Store build will be free with no IAP, subscription, ads, or paid AI quota. | The first release should validate long-term personal recording and self-hosted AI usage before introducing monetization. Future paid directions are recorded as candidates only: AI memory/knowledge base, managed AI, deeper reviews/analytics, advanced search/organization, sync/backup/migration, and export/print products. |
| 017 | CloudKit private sync is now included in first-phase App Store readiness with one user-visible `iCloud Sync` switch and a complete-moment sync goal. | Users should not choose between structural data and media; after enabling sync, moments should eventually be complete across devices, including text, comments, tags, check-ins, AI metadata, photos, audio, video, and document attachments. Metadata-only states are implementation intermediates, not product modes. App Store metadata should only market iCloud Sync after the final build passes those gates. |
| 017 | CloudKit first-device migration and conflict rules are intentionally simple. | First enable treats the current iPhone as the initial archive and uploads all active local data. Empty additional devices download the iCloud archive automatically. Non-empty additional devices do not auto-merge in v1. Conflicts are resolved automatically: newer `updatedAt` wins for ordinary fields, deletes win, comments/tags merge by stable IDs, media syncs by media ID, and AI metadata follows the newest generation/status. |
| 017 | CloudKit App Store readiness depends on owner real-device UAT, not external TestFlight user volume. | The owner can validate P0 gates with two real devices under the same Apple ID. External TestFlight is useful later for unfamiliar-user onboarding, but the first App Store decision can rely on documented owner UAT covering complete upload, empty-device restore, offline catch-up, update/delete sync, complete media, non-empty device protection, iCloud unavailable states, quota/failure handling, reinstall restore, secret exclusion, and copy consistency. |
| 017 | CloudKit private sync boundary is frozen before implementation. | The release identity follows the long-running daily app bundle, UAT remains isolated, App Store v1 waits for completed CloudKit sync, user-visible library content plus drafts/preferences/tombstones sync, secrets/provider config/raw AI inputs/local diagnostics/sync queues/cache/sample/UAT data do not sync, Media Quality is a synced preference with optimized default and original-quality option, Timeline stays free of per-moment sync badges, and Settings owns low-frequency status plus `Sync Now`. |
| 017 | v1 App Store metadata uses one unified brand plus localized positioning. | Store name is `Ownlight` in both Simplified Chinese and English metadata, home-screen display name is also `Ownlight`, zh-Hans subtitle is `自己的私密时间线`, and English subtitle is `Your private timeline`. Do not create a separate Chinese brand name for v1; use Chinese metadata to explain the private timeline positioning instead. |
| 017 | v1 skips TestFlight and uses direct App Store submission. | The owner has already validated the daily iPhone and iPad paths; there is no active external tester pool. TestFlight would add beta metadata and feedback-flow overhead without materially improving v1 quality. Final control is owner release-grade UAT, App Store review, and manual release after approval. |
| 017 | CloudKit implementation starts with account-status scaffold only. | The first checkpoint should configure CloudKit capability safely, add overrideable container config and account status detection, and keep Settings informational. It must not create CloudKit records, expose a sync toggle, change SQLite schema, or give the Share Extension CloudKit entitlement. |
| 017 | CloudKit local metadata, pending queue, and record mapper foundation are implemented, but sync is still not enabled. | iOS now has independent `local_cloudkit_*` SQLite tables, deterministic local record state naming, a CloudKit-only pending-change queue that does not reuse the Mac/server outbox, due-work claim/retry/finish helpers, and pure payload mappers for moments, media metadata, comments, tags, tag aliases, post-tag assignments, timeline AI summaries, check-in items, check-in entries, check-in media metadata, and check-in AI summaries. These mappers deliberately exclude local paths, server sync runtime, raw transcript text, provider/model/token/error diagnostics, and queue/cache state. This still does not create CloudKit records, upload/download user content, expose an `iCloud Sync` toggle, or change the App Store privacy label decision. |
| 017 | CloudKit sync runner skeleton is implemented, but real CloudKit transport is still not enabled. | iOS now has a testable runner that claims due pending changes, sends upserts/deletes to an abstract transport, writes returned record metadata to local record state, marks successful work finished, and schedules failed work for retry. Current tests use fake transport only; this still does not create real `CKRecord`s, connect to iCloud, upload/download user content, expose an `iCloud Sync` toggle, or change the App Store privacy label decision. |
| 017 | CloudKit CKRecord encoder is implemented, but records are still not saved to iCloud. | iOS can now convert the safe payload model into in-memory `CKRecord`s with deterministic record type/name/zone and only the allowed scalar/list field values. This proves the local mapper output can cross the CloudKit SDK boundary, but it still does not create CloudKit zones/schema, call CloudKit databases, upload/download user content, expose an `iCloud Sync` toggle, or change the App Store privacy label decision. |
| 017 | CloudKit default transport and zone preparation are implemented, but not wired into product sync yet. | iOS now has a `CloudKitDatabaseWriting` adapter boundary, a cached `CloudKitZonePreparer`, and `CloudKitDefaultSyncTransport` for save/delete. Tests verify zone creation is de-duped, save prepares the zone before saving an encoded record, delete prepares the zone before deleting a record ID, and save returns deterministic payload snapshot metadata. This still is not called by Settings or background sync by default, tests use fake database only, no real CloudKit writes have run, user content is not uploaded/downloaded, and the App Store privacy/sync copy remains unchanged. |
| 017 | CloudKit local payload resolver is implemented for the current local-first entity set. | `CloudKitLocalPayloadResolver` can resolve queued local changes from SQLite into safe payloads for moments, media metadata, comments, tags, tag aliases, post-tag assignments, check-in items, check-in entries, check-in media metadata, timeline AI summaries, and check-in AI summaries. Missing entities resolve to nil rather than inventing upload content. Weekly reviews, preferences, drafts, media asset bytes, remote pull/apply, and product enablement still remain future slices. |
| 017 | CloudKit CKRecord decoder is implemented as a download/apply prerequisite, but CloudKit pull is still not enabled. | `CloudKitRecordDecoder` can decode known Ownlight `CKRecord`s back into `CloudKitRecordPayload` field values and rejects unsupported record types or record names outside the deterministic `pm.<entity>.<id>` namespace. Tests run entirely against in-memory records; no CloudKit query, subscription, fetch, or apply pipeline is active yet. |
| 017 | CloudKit incoming record policy is implemented as a pure download/apply guard, but remote apply is still not enabled. | `CloudKitIncomingRecordPolicy` classifies decoded remote upserts and deletes before any local SQLite write: pending local changes defer remote work, remote deletes apply when safe, already-applied payload snapshots are ignored, and mismatched record identity is rejected. This protects local-first edits and keeps idempotency testable before adding CloudKit fetch/cursor and entity-specific DB apply code. |
| 017 | CloudKit zone changes transport is implemented behind the testable adapter boundary, but remote apply is still not enabled. | `CloudKitDefaultSyncTransport` can fetch private-zone changes, decode modified records into safe payloads, decode deletion events into deterministic entity identities, and return an opaque server change token as `Data`. This proves the CloudKit download SDK boundary without querying real iCloud in tests, persisting cursors, writing SQLite, exposing a sync toggle, or changing App Store privacy copy. |
| 017 | CloudKit sync cursor persistence is implemented locally, but pull/apply is still not enabled. | `local_cloudkit_sync_state` now has a tested DAO for per-scope server change token storage and failure bookkeeping. Successful future pulls can advance the cursor only after apply, while failures keep the previous token. This still does not fetch real iCloud data, apply remote records, expose a sync toggle, or change App Store privacy copy. |
| 017 | CloudKit pull runner dispatch is implemented behind an injected apply boundary. | `CloudKitSyncRunner.pullOnce` now fetches zone changes through the transport, applies incoming policy decisions, defers remote records that collide with unfinished local pending changes, records downloaded snapshots/deletes in local CloudKit record state after the injected applier succeeds, and advances the cursor only when safe. Tests use fake transport; no real iCloud, media transfer, Settings toggle, or App Store privacy copy changes are active. |
| 017 | CloudKit local DB apply supports moment/comment records only. | `CloudKitLocalRecordApplier` can apply remote moment/comment upserts and deletes directly to `local_posts` / `local_comments` as `synced` rows without writing the Mac/server outbox. This proves the first business-table apply boundary and prevents remote changes from echoing back through legacy sync. Tags, media metadata/assets, check-ins, AI artifacts, drafts, preferences, weekly reviews, non-empty library convergence, real CloudKit smoke, and user-visible enablement remain future slices. |
| 017 | CloudKit local DB apply now supports tag metadata records. | `CloudKitLocalRecordApplier` can apply remote topic tag, tag alias, and post-tag assignment upserts/deletes directly to local tag tables without writing the Mac/server outbox. Tag deletes reuse the existing local hard-delete semantics; tag alias and post-tag assignment deletes preserve tombstones. Media metadata/assets, check-ins, AI artifacts, drafts, preferences, weekly reviews, non-empty library convergence, real CloudKit smoke, and user-visible enablement remain future slices. |
| 017 | CloudKit local DB apply now supports moment media metadata records. | `CloudKitLocalRecordApplier` can apply remote `PMMedia` metadata upserts/deletes directly to `local_media` without writing the Mac/server outbox. This covers media rows and delete tombstones only; media asset bytes/download queues, check-ins, AI artifacts, drafts, preferences, weekly reviews, non-empty library convergence, real CloudKit smoke, and user-visible enablement remain future slices. |
| 017 | CloudKit local DB apply now supports check-in metadata records. | `CloudKitLocalRecordApplier` can apply remote `PMCheckInItem`, `PMCheckInEntry`, and `PMCheckInMedia` metadata upserts/deletes directly to local check-in tables without writing the Mac/server outbox. Optional missing `tagId` on check-in items is dropped instead of blocking pull; check-in entry/media still require existing parents. This covers metadata/tombstones only; media asset bytes and AI artifacts/preferences/drafts/weekly reviews/non-empty library convergence/real CloudKit smoke/user-visible enablement remain future slices. |
| 017 | CloudKit local DB apply now supports generated AI artifact records. | `CloudKitLocalRecordApplier` can apply remote Timeline AI summary and Check-in AI summary display payloads/deletes directly to local summary tables without writing the Mac/server outbox. The CloudKit payload still excludes raw transcript text and provider/model/token/error diagnostics; remote apply requires existing parent post/media or check-in entry/media rows. Preferences, drafts, weekly reviews, media asset bytes, non-empty library convergence, real CloudKit smoke, and user-visible enablement remain future slices. |
| 017 | CloudKit mapper/resolver/apply now supports Weekly Review records. | `CloudKitRecordMapper` can map local `ReviewPayload` into `PMWeeklyReview`, `CloudKitLocalPayloadResolver` can resolve queued weekly review changes from `AppSettings.localWeeklyReviews`, and `CloudKitLocalRecordApplier` can merge remote weekly review upserts/deletes into local weekly reviews without writing the Mac/server outbox. Provider/model/error diagnostics remain excluded from CloudKit; preferences, drafts, media asset bytes, non-empty library convergence, real CloudKit smoke, and user-visible enablement remain future slices. |
| 017 | CloudKit mapper/resolver/apply now supports the app Preference singleton record. | `CloudKitPreferenceSnapshot` defines a fixed allowlist for user-visible app preferences and maps it into `PMPreference` record `pm.preference.app`. The resolver can map queued preference upserts from current `AppSettings`, and the local applier can apply remote preference upserts without writing the Mac/server outbox. Environment, credential, runtime, diagnostics, onboarding/sample, and legacy Mac/server sync fields are deliberately excluded; draft sync, media asset bytes, non-empty library convergence, real CloudKit smoke, and user-visible enablement remain future slices. |
| 017 | CloudKit mapper/resolver/apply now supports Draft metadata records. | `CloudKitDraftSnapshot` maps the composer singleton and edit draft records into `PMDraft`. The resolver can map current composer/edit draft text and timestamps from local draft stores, and the local applier can apply remote draft upserts/deletes without writing the Mac/server outbox. Draft v1 syncs text, occurredAt, updatedAt, and edit existing media IDs; new draft media bytes, temporary draft files, local paths, media asset transfer, real CloudKit smoke, and user-visible enablement remain future slices. |
| 017 | CloudKit media asset upload has a dedicated runner/transport boundary. | `asset_upload` no longer reuses ordinary metadata save. `CloudKitLocalPayloadResolver` now emits `CloudKitAssetRecordPayload` only for existing app-owned media files, `CloudKitRecordEncoder` attaches those files as `CKAsset`s, `CloudKitDefaultSyncTransport.saveAssets` prepares the private zone before saving asset records, and `CloudKitRecordDecoder` skips asset fields during metadata decode. This covers upload boundary only; downloaded asset materialization, real CloudKit smoke, and user-visible enablement remain future slices. |
| 017 | CloudKit media asset download materialization is implemented behind fake-transport-tested pull. | `CloudKitDefaultSyncTransport.fetchChanges` keeps `CKAsset` fields alongside decoded payloads, `CloudKitSyncRunner.pullOnce` applies assets after metadata and before cursor advancement, and `CloudKitLocalRecordApplier` copies moment/check-in media assets into app-owned media storage without writing the Mac/server outbox. Real iCloud smoke, quota/failure UX, Settings enablement, and UAT remain future gates. |
| 017 | Ordinary Moment create/update/delete enters the CloudKit pending queue behind `iCloud Sync`. | After real-device smoke passed on both iPhone and iPad, ordinary Timeline create/update/delete now enqueue `.moment` upsert/delete pending changes only when `iCloud Sync` is enabled. Later checkpoints added media metadata/assets and first-device initial-library queue preparation; the guarded smoke path still remains one-record only. |
| 017 | CloudKit first-device initial local-library upload is implemented as a one-time queue-preparation step. | First sync after enabling `iCloud Sync` treats the current iPhone as the initial archive and queues the allowed existing local data into CloudKit pending changes under `cloudkit_initial_upload_v1`. It excludes welcome/sample data, credentials, raw transcript text, diagnostics, sync runtime/cache, provider profiles/API keys, and legacy server state. Full iPhone/iPad UAT, real-device non-empty second-device validation, and release privacy copy remain gates before this can be marketed as a completed App Store sync feature. |
| 017 | CloudKit first sync protects non-empty second devices before initial upload. | Before `cloudkit_initial_upload_v1`, the coordinator probes the private CloudKit zone. If CloudKit already has real archive records and the current device also has real local content, v1 blocks and records `cloudkit_initial_upload_conflict` instead of merging. Empty/sample-only devices skip local upload preparation and pull the remote archive first. This keeps the first release's no-merge rule enforceable in code; manual real-device UAT for the non-empty warning path still remains before closing the CloudKit gate. |
| 017 | CloudKit manual `Sync Now` must drain batches and `0/0/0` is not a completeness proof. | A sync round can report no processed uploads/downloads/deletes while another device is still incomplete if previous work is blocked, already claimed, deferred, or the remote page dependency order caused a pull abort. Manual sync now loops through multiple upload/pull batches and the operator should inspect `local_cloudkit_sync_state`, pending queue, and two-device counts when an iPad remains behind. |
| 017 | CloudKit pull must tolerate dependency order and sanitized legacy draft identifiers. | CloudKit zone changes can expose a child record before its parent in the same or later page, and older draft records may use `edit_...` record IDs without a `postId` field. Pull now fetches a deterministic missing parent before retrying the child and can recover edit draft post identity from sanitized record names, keeping the cursor from being blocked by recoverable ordering/compatibility issues. |
| 017 | CloudKit pull treats orphaned optional derived records as ignorable, not cursor blockers. | Optional derived records such as post-tag assignments, tag aliases, Timeline AI summaries, and Check-in AI summaries can reference parent records that were deleted, never uploaded, or cannot be applied from historical CloudKit data. The pull runner now tries to fetch/apply the parent first; if that still fails for these derived records, it records one ignored item and advances the cursor instead of blocking the whole second-device restore. Core content records remain strict. |
| 017 | CloudKit full reconciliation runs once with an independent cursor after normal sync succeeds. | A device can have an already-advanced normal CloudKit cursor while still missing older optional derived records because previous builds skipped, ignored, or failed to apply them. `cloudkit_full_reconcile_v1` performs a one-time full private-zone pull after a clean sync and stores its own cursor, recovering historical AI summaries, tags, and other safe records without resetting the normal sync token or re-uploading secrets. |
| 017 | App Store v1 originally launched with Simplified Chinese primary metadata, English secondary localization, existing Cloudflare domain for public URLs, and category primary `Lifestyle` / secondary `Productivity`; post-release availability is now overseas-first while China mainland strategy is unresolved. | The first submitted release targeted China mainland, but App Store visibility and ICP requirements made China mainland distribution a separate follow-up. The live app was switched to 174 countries or regions on 2026-07-02, with China mainland unchecked. The app runtime can continue following system language with English and Simplified Chinese support. Apple does not require a dedicated domain for Privacy Policy or Support URL, so a stable HTTPS product path/subdomain on the existing Cloudflare domain is sufficient for v1. `Ownlight` reads first as private life recording / private moments, so `Lifestyle` is the better primary category; `Productivity` remains secondary for organization, search, capture, and review. Avoid Social Networking, Health & Fitness, and Medical. |
| 017 | Post-release availability is temporarily moved outside China mainland while ICP/China mainland listing strategy is unresolved. | China mainland App Store visibility now depends on regulatory/ICP handling that does not match the current static Cloudflare-only product setup. The live app was switched to 174 countries or regions and China mainland was unchecked on 2026-07-02. Because v1 `1.0 Ready for Distribution` screenshot and subtitle surfaces appear locked, fully English store media/metadata should be handled through a new version if needed. |
| 017 | App Store v1 first launch onboarding is a short welcome page plus a local welcome sample moment, not a multi-step tutorial or settings wizard. | New users need to understand the product as a private timeline first, not learn every feature or AI configuration. The welcome page appears only once per app installation and only says the app is a private, local-first space. Empty-library users then see a top-of-today welcome sample moment that teaches through the real Timeline shape: welcome text, sample audio block, sample generated AI summary, tags, one comment, and one-time gesture hint. The sample is teaching data only: it is marked internally, deleted forever when removed, and excluded from sync, export, reviews, and future CloudKit. |
| maintenance | 2026-05-25 security checkpoint: Admin high-privilege routes now require persisted `Device.adminEnabled`; ordinary iOS tokens retain sync and reduced diagnostics access only. Media upload paths validate safe IDs and final containment; media reads/import/restore reject symlinks, hard links, non-regular files, and realpath escapes. iOS rejects non-loopback HTTP Server URLs and constrains downloaded media cache filenames. | Security review found concrete server filesystem/auth boundaries and iOS token/cache risks. Fixes keep the local-first product shape while tightening network and filesystem trust boundaries. |
| 013 | `.planning/` 恢复为仓库唯一的结构化事实源。 | 这样可以和其他 stock GSD 项目保持一致，减少双轨维护带来的编号和认知漂移。 |
| 013 | 2026-05-13 Weekly Review sparse-recovery and copyability follow-up: server no longer treats `notableMoments` as a hard requirement for a good review draft. The `empty_review_content` quality gate now weights `bodyMarkdown` more heavily, so a strong long-form review with a real title and keywords can pass without being mislabeled as `内容过稀`; sparse recovery still applies when the body is actually too thin. On live server port `3210`, a new review generated through `weekly-review-v3` confirmed the earlier partial-recovery warning was a false-positive threshold issue rather than an active provider outage. iOS Weekly Review detail now renders `bodyMarkdown` through a selectable native text view and exposes an explicit `Copy text` action in the toolbar menu; keywords, notable notes, and uncertainty bullets also opt into text selection. Validation covered server tests/build, generic iOS build, live server restart, real review smoke, and real-device build/install without preserving personal endpoint or device identifiers in planning state. | 当前状态应直接记录在 `.planning/STATE.md`，而不是依赖另一个规划系统。 |
| 016 | 下一阶段把热点文件瘦身、Sync Doctor 引导化、Archive promote/restart 收口和恢复路径 focused regression tests 合并成一个统一 phase。 | 这四件事都服务于同一个目标：降低维护风险、缩短故障恢复路径，并且比再加一个新功能模块更符合当前 v0.1 阶段的真实价值。 |

## Blockers

- Open-source release is active and uses a clean snapshot rather than private repository history. The release must exclude signing artifacts, local overrides, credentials, private runtime data, and `.planning/phases/`; GitHub Release remains source-only.
- M017 当前重点是发布后监控 Ownlight v1 的 App Store listing 和首个 App Store 安装版本 smoke。单机 iPhone 仍是 baseline；CloudKit private sync 第一阶段已经关闭 UAT gate。Apple Developer release App IDs、App Group、iCloud container、Debug 真机签名安装、真实 iPhone/iPad guarded smoke、普通 Moment/media/comment/summary/tag/check-in/draft/preference sync、delete/tombstone、edit-media add/remove、用户可读 failure messages、secrets/provider config exclusion spot-check、container count/queue/SQLite health 检查，以及一次性 full reconciliation 自愈机制均已进入当前 checkpoint。`npm run doctor:app-store` 覆盖源代码层面的上架/隐私 preflight，并在 2026-06-24 后禁止首发版声明 `UIBackgroundModes = audio`。2026-06-11 已上传并选择 App Store build `1.0 (2)`，App Privacy 已发布为 `Data Not Collected`；2026-06-21 已完成 Age Rating `4+`、free pricing、China mainland-only availability、Mac/Vision distribution off、iPhone `6.5" Display` screenshots `6 of 10` 上传，并在 owner 确认后提交审核；2026-06-24 审核被 Guideline 2.5.4 打回，同日 build `1.0 (3)` 已上传、切换为当前审核 build，并重新提交；2026-06-25 build `1.0 (3)` 被 Guideline 3.1.1 打回，2026-06-27 已在 App Store Connect 回复澄清，Apple 随后接受并批准；2026-06-28 22:35 CST 已手动发布，App Store Connect 状态为 `1.0 Ready for Distribution`；2026-07-02 已从 China mainland-only 改为 174 countries/regions 可用、China mainland `Not Available`，变更预计 24 小时内生效。Push-assisted near-realtime sync 记录在 Backlog B002，不作为当前 CloudKit 发布门禁。

## Session

Last Date: 2026-07-17
Stopped at: Ownlight iOS `1.1 (4)` is `Waiting for Review` with English (U.S.) as Primary Language and six ordered English screenshots. The next action is publishing the audited clean-history source snapshot and `v1.1.0` source-only GitHub Release at `Popcornnnnnnnn/ownlight`, then monitoring App Review.
Resume File: docs/APP-STORE-READINESS.md
